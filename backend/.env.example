# Copy this to .env and fill in your values
# The API key can also be sent per-request from the frontend

# Optional: set a default key server-side (keeps key off the client)
# ANTHROPIC_API_KEY=sk-ant-api03-...